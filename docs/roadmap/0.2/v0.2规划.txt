主题: prompt 编辑体验优化
期望的功能(在现有prompt edit体验的基础上):
粘贴图片
快捷语输入(要不要做自动约束识别与补全?) 例: 右键/斜杠/某快捷字符后 自动显示/补全快捷约束文本(先口头汇报/写一个简短的方案到XXX目录/只改代码不用编译)
skill 快捷引用(复刻tui的$行为)
全局快捷键唤起应用并置焦点于输入框
快捷文件输入(优先引用git有修改的文件/最近有更改的文件之类的)
草稿保存? 会不会侵入到并行?
结构化prompt模板 Goal / Context / Constraints / Done when
review和prompt编辑的联动 比如一键让aireview之类的 不过我不太喜欢用ai review 这个存疑
session 命名 可能有用 但是不知道怎么做能有用但是不麻烦
发送检查 有点可疑

顺序
1. 全局快捷键 降低切换成本 先做成将应用切换至前台 焦点之后再说
2. 统一输入框为一个控件
3. 补全框架 后续的快捷语/skill/快捷文件都走同一框架
3.1 快捷键唤起悬浮框
4. 快捷语
5. skill
6. 快捷文件
7. 结构化模板 做成右键action
8. 粘贴图片

明确后续版本进行:
session 命名和草稿 明确延后至'并行'阶段