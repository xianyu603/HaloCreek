# Designing Rules

0. 编号更小的规则优先级更高。
1. 设计或编码前先读相关代码，以代码事实为准。
2. 可以用非核心行为的适当降级换取代码明显精简，记录降级内容和用户可感知影响即可。
3. 模块应保持明确边界。类/方法/参数应语义明确，避免必须阅读实现才知道其定位、输入输出或行为边界。
4. 禁止猜接口或静默降级；外部协议、文件格式、API 字段取不到就留空、失败或显式报告。
5. 短且线性的单点调用不要拆成很多私有方法，禁止很薄的转发方法。
